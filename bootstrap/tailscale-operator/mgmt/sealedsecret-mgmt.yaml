# This file is generated by: task secrets:seal:tailscale:mgmt
# It is safe to commit — values are encrypted with the mgmt cluster's public key.
# Regenerate whenever you rotate the Tailscale OAuth credentials.
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: operator-oauth
  namespace: tailscale
spec:
  encryptedData:
    client_id: REPLACE_ME
    client_secret: REPLACE_ME
  template:
    metadata:
      name: operator-oauth
      namespace: tailscale
    type: Opaque
